_schema-version: "3.2"
ID: PGOmedioambiente
version: 0.0.1
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm install
        - npm run build:workspaces
modules:
  - name: PGOmedioambiente-destination-content
    type: com.sap.application.content
    requires:
      - name: PGOmedioambiente_uaa
        parameters:
          service-key:
            name: PGOmedioambiente_uaa-key
      - name: PGOmedioambiente_html_repo_host
        parameters:
          service-key:
            name: PGOmedioambiente_html_repo_host-key
      - name: PGOmedioambiente-destination-service
        parameters:
          content-target: true  
      - name: pgo_PGOmedioambiente_workflow
        parameters:
            service-key:
              name: pgo_PGOmedioambiente_wf-key    
    parameters:
      content:
        instance:
          destinations:
            - Name: my_service_PGOmedioambiente_html_repo_host
              ServiceInstanceName: PGOmedioambiente_html_repo_host
              ServiceKeyName: PGOmedioambiente_html_repo_host-key
              sap.cloud.service: com.aysa.pgo
            - Authentication: OAuth2UserTokenExchange
              Name: my_service_uaa_PGOmedioambiente
              ServiceInstanceName: PGOmedioambiente_uaa
              ServiceKeyName: uaa_PGOmedioambiente-key
              sap.cloud.service: com.aysa.pgo
            - Name: bpmworkflowruntime
              Authentication: OAuth2JWTBearer
              ServiceInstanceName: pgo_sancion_workflow
              ServiceKeyName: wf_pgo_sancion-key
          existing_destinations_policy: update
    build-parameters:
      no-source: true
  - name: PGOmedioambiente_ui_deployer
    type: com.sap.application.content
    path: .
    requires:
      - name: PGOmedioambiente_html_repo_host
        parameters:
          content-target: true
    build-parameters:
     build-result: resources
     requires:
       - name: uimodule
         artifacts:
           - dist/uimodule.zip
         target-path: resources/
  - name: uimodule
    type: html5
    path: uimodule
    build-parameters:
      builder: custom
      commands:
        - npm run build:uimodule --prefix ..
      supported-platforms: []
resources:
  - name: PGOmedioambiente-destination-service
    type: org.cloudfoundry.managed-service
    parameters:
      service: destination
      service-name: PGOmedioambiente-destination-service
      service-plan: lite
      config:
        HTML5Runtime_enabled: true
        version: 1.0.0
  - name: PGOmedioambiente_html_repo_host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-host
  - name: PGOmedioambiente_uaa
    type: org.cloudfoundry.managed-service
    parameters:
      path: ./xs-security.json
      service: xsuaa
      service-plan: application
  - name: pgo_PGOmedioambiente_workflow
    type: org.cloudfoundry.managed-service
    parameters:
      service: workflow
      service-plan: standard